import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF2E7D32);       // Зелёный
  static const primaryLight = Color(0xFF60AD5E);  // Светло-зелёный
  static const primaryDark = Color(0xFF005005);   // Тёмно-зелёный
  static const secondary = Color(0xFF66BB6A);     // Мягкий зелёный
  static const background = Color(0xFFFFFFFF);    // Белый
  static const surface = Color(0xFFF5F5F5);       // Светло-серый
  static const textPrimary = Color(0xFF1A1A1A);   // Почти чёрный
  static const textSecondary = Color(0xFF757575); // Серый
  static const error = Color(0xFFE53935);
  static const success = Color(0xFF43A047);
}
